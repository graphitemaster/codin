package main

// distinct
// struct
// union
// enum
// attributes
// switch
// expression directives (partial, assert, panic, unroll)
// procedure groups
// [] indexing
// foreign declarations
// implicit selector expressions
// using statement

foo :: proc() -> int {
  return 0;
}